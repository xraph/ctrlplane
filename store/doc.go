// Package store defines the aggregate persistence interface that composes
// all domain-specific store interfaces. Implementations include in-memory
// (for testing), PostgreSQL (production), and SQLite (dev/standalone).
package store
