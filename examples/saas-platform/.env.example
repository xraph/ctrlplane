# =============================================================================
# SaaS Platform Example — Environment Variables
# =============================================================================
# Copy this file to .env and adjust values for your environment.
#
#   cp .env.example .env
#
# Then run:
#
#   go run ./examples/saas-platform
#
# =============================================================================

# ---------------------------------------------------------------------------
# Store backend: memory (default), bun, badger, mongo
# ---------------------------------------------------------------------------
CP_STORE=memory

# ---------------------------------------------------------------------------
# Bun (PostgreSQL / SQLite) store settings
# Only used when CP_STORE=bun
# ---------------------------------------------------------------------------
# CP_BUN_DRIVER=postgres
# CP_BUN_DSN=postgres://user:pass@localhost:5432/ctrlplane?sslmode=disable

# For SQLite:
# CP_BUN_DRIVER=sqlite
# CP_BUN_DSN=file:ctrlplane.db?cache=shared&_pragma=journal_mode(WAL)

# ---------------------------------------------------------------------------
# Badger store settings
# Only used when CP_STORE=badger
# ---------------------------------------------------------------------------
# CP_BADGER_PATH=./data/badger

# ---------------------------------------------------------------------------
# MongoDB store settings
# Only used when CP_STORE=mongo
# ---------------------------------------------------------------------------
# CP_MONGO_URI=mongodb://localhost:27017
# CP_MONGO_DATABASE=ctrlplane

# ---------------------------------------------------------------------------
# Ctrl Plane API base path
# ---------------------------------------------------------------------------
CP_BASE_PATH=/api/cp

# ---------------------------------------------------------------------------
# Docker provider
# ---------------------------------------------------------------------------
# CP_DOCKER_HOST=unix:///var/run/docker.sock
CP_DOCKER_NETWORK=bridge

# ---------------------------------------------------------------------------
# Seed demo tenants on startup (true/false)
# ---------------------------------------------------------------------------
CP_SEED=true
